# ci.yml — gate this package the way the gateway and agent-utils are gated.
#
# This repo had publish.yml and registry.yml but nothing that RAN the server
# before shipping it, so the only test of a release was npm-publishing it and
# waiting. That is how an argument-validation hole reached 1.5.0: every tool
# declared inputSchema.required, nothing enforced it, and a call missing an
# argument fetched `?symbol=undefined` — spending a free-tier call, or real
# USDC with DEX_WALLET_KEY set, on a query that could not return an answer.
#
# Nothing here touches the network: the assertions are about calls that must
# NOT be made, so CI cannot go red because a free tier is spent or an upstream
# is slow.

name: ci

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: syntax check (every tracked .js/.cjs/.mjs)
        run: |
          set -euo pipefail
          files=$(git ls-files '*.js' '*.cjs' '*.mjs')
          [ -n "$files" ] || { echo "::error::no source files matched — the glob is wrong"; exit 1; }
          echo "$files" | while read -r f; do node --check "$f"; done
          echo "checked $(echo "$files" | wc -l) file(s)"

      # ★ The published package.json is what the MCP registry reads to prove npm
      #   ownership. If mcpName drifts from the registry namespace, publishing
      #   succeeds and the registry entry silently stops resolving to this
      #   package — a one-way door that is far cheaper to catch here.
      - name: mcpName matches the canonical registry namespace
        run: |
          set -euo pipefail
          NAME=$(node -p "require('./package.json').mcpName || ''")
          [ "$NAME" = "com.donnyautomation/dex-data" ] || {
            echo "::error::package.json mcpName is '$NAME', expected com.donnyautomation/dex-data"; exit 1; }
          echo "mcpName: $NAME"

      # ★ The version lives in FOUR places and they drift silently.
      #
      #   Bumping to 1.5.1 updated package.json and both server.json fields but
      #   not serverInfo in server.mjs, so the server would have introduced
      #   itself over the MCP protocol as 1.5.0 while npm served 1.5.1 — the one
      #   version a CLIENT actually observes, reporting the release that has the
      #   bug rather than the one that fixes it. publish.yml catches this, but
      #   only at the moment of release; catching it here means a mismatch never
      #   reaches a release attempt.
      - name: all four version sites agree
        run: |
          node -e '
            const fs = require("fs");
            const pkg = JSON.parse(fs.readFileSync("package.json", "utf8")).version;
            const sj = JSON.parse(fs.readFileSync("server.json", "utf8"));
            const src = fs.readFileSync("server.mjs", "utf8");
            // serverInfo reads a single VERSION const (since 1.6.0), so the
            // user-agent the gateway attributes on and the version a client sees at
            // handshake cannot drift apart. Check that const, and assert serverInfo
            // still refers to it rather than a re-hardcoded literal.
            const info = (src.match(/^const VERSION = ["\x27]([^"\x27]+)["\x27];/m) || [])[1];
            if (!info) { console.error("could not find the VERSION const in server.mjs"); process.exit(1); }
            if (!/serverInfo:\s*\{[^}]*version:\s*VERSION\b/.test(src)) {
              console.error("serverInfo must read the VERSION const, not a hardcoded literal"); process.exit(1);
            }
            const seen = { "package.json": pkg, "server.json": sj.version, "server.json packages[0]": sj.packages?.[0]?.version, serverInfo: info };
            const bad = Object.entries(seen).filter(([, v]) => v !== pkg);
            if (bad.length) { console.error("VERSION MISMATCH:", seen); process.exit(1); }
            console.log(`OK: all four report ${pkg}`);
          '

      # ★ The MCP registry caps server.json description at 100 characters and
      #   rejects the whole publish with a 422 if it is longer. Nothing local
      #   said so: package.json has no such limit, the JSON schema does not
      #   express it, and `mcp-publisher validate` only learns it by asking the
      #   registry. So a description written for npm sailed through every check
      #   here and failed at the registry AFTER npm had already published — the
      #   two halves of a release out of step, with agents browsing the registry
      #   still being pointed at the previous version. Assert it locally so the
      #   limit is discovered at commit time, not at release time.
      - name: server.json description fits the MCP registry limit
        run: |
          node -e '
            const d = JSON.parse(require("fs").readFileSync("server.json", "utf8")).description || "";
            console.log(`server.json description: ${d.length} chars`);
            if (d.length > 100) {
              console.error(`::error::server.json description is ${d.length} chars; the MCP registry rejects anything over 100 with a 422`);
              process.exit(1);
            }
          '

      - name: argument validation over real stdio JSON-RPC (no network)
        run: node test/arg-validation.test.mjs
